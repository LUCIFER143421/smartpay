services:
  - type: web
    name: smartpay-api
    env: docker
    rootDir: smartpay
    dockerfilePath: Dockerfile
    plan: free
    healthCheckPath: /actuator/health
    envVars:
      - key: DB_URL
        value: jdbc:postgresql://$(RENDER_DB_HOST):$(RENDER_DB_PORT)/$(RENDER_DB_NAME)
      - key: RENDER_DB_HOST
        fromDatabase:
          name: smartpay-db
          property: host
      - key: RENDER_DB_PORT
        fromDatabase:
          name: smartpay-db
          property: port
      - key: RENDER_DB_NAME
        fromDatabase:
          name: smartpay-db
          property: database
      - key: DB_USERNAME
        fromDatabase:
          name: smartpay-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: smartpay-db
          property: password
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRATION
        value: "86400000"
      - key: OLLAMA_BASE_URL
        value: "http://localhost:11434"

databases:
  - name: smartpay-db
    databaseName: smartpay
    user: smartpay_user
    plan: free